# URL of the instance registry.
# Can be a local path (./registry.json) or a remote URL.
REGISTRY_URL=./registry.json

# Initial map view
MAP_ZOOM=5
MAP_MIN_ZOOM=4
MAP_CENTER=10.5,51.2   # lon,lat — centre of Germany by default

# Host port
APP_PORT=8090
